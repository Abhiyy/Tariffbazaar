
<div id="mrova-feedback" style="margin-top: -239.5px; top: 50%; display: block; height: 479px; right: -462px;">
    
    <div id="mrova-form">
        <form class="form-horizontal">
           
                    <label for="mrova-name">Your Name*</label> <input type="text" value="" id="txtName" class="form-control" >
                    <label for="mrova-email">Email*</label> <input type="email" value="" id="txtEmail" class="form-control" >
                    <label for="mrova-message">Message*</label>
                    <textarea cols="35" rows="5" id="txtMessage" class="required" ></textarea>
            <br /><br />
            <button id="btnSend" class="btn btn-default" >Send</button>
        </form>
    </div>
    <div id="mrova-img-control" style="margin-top: -84px; top: 50%;"></div>
</div>

<script type="text/javascript">


    $(document).ready(function () {
        $('#btnSend').click(function () {
           
            var user = $('#txtName').val();
            var email = $('#txtEmail').val();
            var message =  $('#txtMessage').val();
            if (user.length > 0 || pswd.length > 0 || message.length > 0 ) {

                $.ajax({
                    method: "POST",
                    url: '@Url.Action("SendMessage","Home")',
                    data: { User: user, Email: email, Message:message },
                    dataType: "json",
                    success: function (response) {
                        if (response.msg == 'ok') {
                            //alert("Msad");
                            return false;
                        //    window.location.href = response.url;

                        } else {
                            alert(msg);
                       //     window.location.href = response.url;
                        }
                    }

                });
            } else {

                alert("Please fill all the necessary fields.");
                return false;
            }
        });
    });

</script>